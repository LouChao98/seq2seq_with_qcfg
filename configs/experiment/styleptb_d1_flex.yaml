# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /datamodule: styleptb
  - override /model: styleptb
  - override /model/tree_encoder: gnn
  - override /model/decoder: d1_flex
  - override /callbacks: default
  - override /logger: wandb
  - override /trainer: default

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

# name of the run determines folder name in logs
name: "styleptb_${hydra:runtime.choices.model/decoder}"

model:
  # load_from_checkpoint: /home/louchao/project/semantic_parsing_qcfg/logs/experiments/runs/styleptb_d1_flex/2022-09-13_22-31-30/checkpoints/last.ckpt
  decoder:
    check_ppl_batch_size: 2
    cpd_rank: 128

trainer:
  max_epochs: 50
  accumulate_grad_batches: 1
  gradient_clip_val: 3

logger:
  wandb:
    project: "styleptb"
    tags:
      - "${hydra:runtime.choices.model/parser}"
      - "${hydra:runtime.choices.model/tree_encoder}"
      - "${hydra:runtime.choices.model/decoder}"
    offline: ${in_cluster:}
